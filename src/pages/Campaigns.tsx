import type { FC } from 'react';
import CampaignListDashboard from '@/components/campaigns/CampaignListDashboard';

const CampaignsPage: FC = () => {
  return <CampaignListDashboard />;
};

export default CampaignsPage;
